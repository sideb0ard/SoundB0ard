let rotrrr = gen() {
	setup() {
	  let pat = ["b3", "c4", "c5", "c6"]
	  let idx = 0
	}

	run() {
	  let ret = pat[idx]
	  let idx = idx + 1
	  if (idx > len(pat) - 1) { let idx = 0}

          return "bd sn*3"
	}
}
