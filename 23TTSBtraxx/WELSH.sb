
let welsh1_gen = gen()
{
  setup()
  {
    let beat = rand_beat();
  }
  run()
  {
    for (i = 0; i < 16; ++i) {
      if (beat[i] == 1) {
        note_on_at(bd, 30, i * pp);
      }
      if (i % 8  == 4) {
        note_on_at(sd, 20, i * pp);
      }
    }
  }
}

