let notz = [55, 54, 51, 50, 48, 47, 55];


let blah_gen = gen()
{
  setup()
  {
    let lenz = [3, 5, 7];
    let lx = 0;
    let beat = bjork(3,8);
  }
  run()
  {
    let mel = combinor(beat, notz);
    notz = rotate(notz, lenz[lx]);
    #print("MEL:", mel);

    play_array(mo, mel, dur=60);

    if (count % 4 == 0) {
      lx = incr(lx, 0, len(lenz));
      beat = bjork(lenz[lx], 8);
    }
  }
}

