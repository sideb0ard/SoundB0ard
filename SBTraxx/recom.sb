let blah_gen = gen()
{
  setup()
  {
    let percbeat = rand_beat();
    let kickbeat = gen_beat();

    let durz = [100, 300, 750];
    let drx = 0;
  }
  run()
  {
    play_array(bd, kickbeat);
    play_array(hh, percbeat);


    play_array(dx, combinor(kickbeat, [44]), dur=durz[drx]);
    drx = incr(drx, 0, len(durz));

    if (count % 3 == 0) {
      percbeat = rand_beat();
    }
    if (count % 4 == 0) {
      kickbeat = gen_beat();
    }
  }
}

