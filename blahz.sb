
let sperl = gen() {
	setup() {
     let idx = 0;
	}

	run() {
		let pattern = "";
    let p = perlin(idx)
		puts(map(p, -1, 1, 0, 100))
    idx = idx + 0.1

    set lp:idx idx
		return pattern;
	}
}
