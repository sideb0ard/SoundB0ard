
let sperl = gen() {
	setup() {
     let n = 0.01;
	}

	run() {
		let pattern = "";
    let p = perlin(n)
    let idx = map(p, -1, 1, 0, 50)
    n = n + 0.1

    set lp:idx idx
		return pattern;
	}
}
